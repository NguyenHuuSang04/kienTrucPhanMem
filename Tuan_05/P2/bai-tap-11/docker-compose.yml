services:
  db-postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - "8083:8080"
    depends_on:
      - db-postgres
