services:
  app:
    build: .
    container_name: pu2-cart
    ports:
      - "8082:8082"
    environment:
      SERVER_PORT: 8082
      REDIS_HOST: ${REDIS_HOST:-192.168.137.98}
      REDIS_PORT: 6379
    restart: unless-stopped
