services:
  app:
    build: .
    container_name: pu1-product
    ports:
      - "8081:8081"
    environment:
      SERVER_PORT: 8081
      REDIS_HOST: ${REDIS_HOST:-192.168.137.98}
      REDIS_PORT: 6379
    restart: unless-stopped
